{
  "name": "synoma-founders",
  "version": "1.1.0",
  "private": true,
  "description": "Motor de contenido IA para clientes del programa Founders",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "pg": "^8.22.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "migrar": "node scripts/migrar.js",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  }
}
